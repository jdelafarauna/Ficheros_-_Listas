Resumen:

La clase Concesionario en Java se encarga de gestionar el inventario de un concesionario de automóviles. 
Permite agregar, ordenar, imprimir y guardar/cargar datos de coches en archivos.

Estructura:

Atributos:
listado: Un ArrayList que almacena objetos de tipo Coche.
nombre: El nombre del concesionario.
numCoches: El número de coches en el inventario.
Métodos:
Constructores:
Concesionario(String nombre): Inicializa el concesionario con el nombre especificado.
Gestión de coches:
anadir(Coche coche): Agrega un coche al inventario.
ImprimirListado(): Imprime la información de todos los coches en el inventario.
imprimirIterando(): Imprime la información de todos los coches en el inventario usando un iterador.
Ordenación:
ordenarPrecioMin(): Ordena los coches por precio ascendente.
ordenarPrecioMax(): Ordena los coches por precio descendente.
ordenarTamanoMax(): Ordena los coches por tamaño descendente.
ordenarTamanoMin(): Ordena los coches por tamaño ascendente.
Manejo de archivos:
Binarios:
escrbirFicheroBinario(String nombreF): Guarda el objeto Concesionario en un archivo binario.
leerFicheroBinario(String nombreF): Carga un objeto Concesionario de un archivo binario.
Texto:
escribirFicheroTexto(String nombreF): Guarda la información de los coches en un archivo de texto.
lecturaFicheroTexto(String nombreF): Lee la información de los coches de un archivo de texto.
